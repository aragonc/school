
<div class="card">
    <div class="card-body">
        <div class="message">
            <h2 class="title">{{ box.message.title }}</h2>
            <div class="info pt-2 pb-3">{{ box.message.info }} - {{ box.message.send_date }}</div>
            <div class="program">{{ box.message.session_title }}</div>
            <div class="content pt-5 pb-2">{{ box.message.content }}</div>
        </div>

    </div>
</div>